services:
  proxy:
    image: nginx:stable
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    volumes:
      - ./proxy/nginxconfig.io:/etc/nginx/nginxconfig.io
      - ./proxy/internal:/etc/nginx/internal
      - ./proxy/conf.d:/etc/nginx/conf.d
      - ./proxy/nginx.conf:/etc/nginx/nginx.conf
      - ./proxy/ssl:/etc/nginx/ssl
      - ./public:/public
      - ./storage/app/recordings:/public/recordings
      - ./internal/startup:/internal/startup:ro
    ports:
      - ${EXTERNAL_WEB_HTTP_PORT:-80}:80
      - ${EXTERNAL_WEB_HTTPS_PORT:-443}:443
      - ${EXTERNAL_WEB_SOCKET_PORT:-6001}:6001
    depends_on:
      - web
      - ws-server
    deploy:
      resources:
         limits:
           memory: 64M
  web:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    privileged: true
    environment:
      - ROLE=server
    volumes:
      - .:/var/www
      - ./internal/limits.ini:/usr/local/etc/php/conf.d/limits.ini:ro
      - /dev/bus/usb:/dev/bus/usb
      - /dev:/dev
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
      - mongo
      - bundler
      # - mariadb
  scheduler:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    privileged: true
    environment:
      - ROLE=scheduler
    volumes:
      - .:/var/www
      - ./internal/limits.ini:/usr/local/etc/php/conf.d/limits.ini:ro
      - /dev/bus/usb:/dev/bus/usb
      - /dev:/dev
    depends_on:
      - redis
      - mongo
      # - mariadb
  serial-scheduler:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    privileged: true
    environment:
      - ROLE=serial-scheduler
    volumes:
      - .:/var/www
      - ./internal/limits.ini:/usr/local/etc/php/conf.d/limits.ini:ro
      - /dev/bus/usb:/dev/bus/usb
      - /dev:/dev
    depends_on:
      - redis
      - mongo
      # - mariadb
  mapper:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    privileged: true
    environment:
      - ROLE=mapper
    volumes:
      - .:/var/www
      - ./internal/limits.ini:/usr/local/etc/php/conf.d/limits.ini:ro
      - /dev/bus/usb:/dev/bus/usb
      - /dev:/dev
      - /sys/class:/sys/class
      - /sys/devices:/sys/devices
      - /run/udev:/run/udev:ro
    depends_on:
      - redis
      - mongo
      # - mariadb
  queue:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    privileged: true
    environment:
      - ROLE=queue
      - QUEUES=default:1,control:1,broadcasts:2,prints,previews,snapshots,recordings # <queue-name>:<min-number-of-workers>
      - SLEEP=5
    volumes:
      - .:/var/www
      - ./internal/limits.ini:/usr/local/etc/php/conf.d/limits.ini:ro
      - /dev/bus/usb:/dev/bus/usb
      - /dev:/dev
    depends_on:
      - redis
      - mongo
      # - mariadb
  ws-server:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    environment:
      - ROLE=ws-server
    volumes:
      - .:/var/www
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
      - mongo
      # - mariadb
  bundler:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    environment:
      - ROLE=bundler
    volumes:
      - .:/var/www
  streamer:
    build: .
    restart: always
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    environment:
      - ROLE=streamer
      - PORT_SCAN_START=60000
      - PORT_SCAN_END=60016
    privileged: true
    volumes:
      - .:/var/www
      - ./internal/limits.ini:/usr/local/etc/php/conf.d/limits.ini:ro
      - /dev/bus/usb:/dev/bus/usb
      - /dev:/dev
      - /sys/class:/sys/class
      - /sys/devices:/sys/devices
      - /run/udev:/run/udev:ro
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
      - mongo
      - bundler
      - proxy
      # - mariadb
      - mapper
  redis:
    image: redis:7.2.5
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    restart: always
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    command: redis-server --save "" --appendonly no
  mongo:
    image: mongo:4.4.6
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    restart: always
    command: mongod --quiet
    volumes:
      - mongo:/data/db
  memcached:
    image: memcached
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    restart: always
  documentation-generator:
    build: .
    logging:
      driver: local
      options:
        max-size: "16m"
        mode: non-blocking
    environment:
      - ROLE=documentation-generator
    volumes:
      - .:/var/www
    ports:
      - 30000:30000
volumes:
  mongo: